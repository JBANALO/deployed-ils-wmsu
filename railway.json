{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "rm -rf dist node_modules/.vite && npm ci && VITE_API_URL=$VITE_API_URL npm run build && cd server && npm ci"
  },
  "deploy": {
    "startCommand": "cd server && node server.js",
    "healthcheckPath": "/api",
    "restartPolicyType": "on-failure",
    "restartPolicyMaxRetries": 10
  }
}
