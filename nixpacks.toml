# Force Node.js provider (prevent Nixpacks from using Caddy static site)
providers = ["node"]

[phases.install]
cmds = ["npm ci", "cd server && npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "cd server && node server.js"
